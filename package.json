{
  "name": "paddrummer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "yarn@4.18.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace @paddrummer/web dev",
    "build": "yarn workspace @paddrummer/web build",
    "preview": "yarn workspace @paddrummer/web preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "yarn workspaces foreach --all --parallel --verbose --exclude paddrummer run typecheck"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^3"
  }
}
